DOCKER_EXEC=podman
DOCKER_ARGS=--storage-opt\ mount_program=/usr/bin/fuse-overlayfs
IMAGE=mmam-builder
PROJECT_NAME=Monster-Monster-Asteroid-Madness
MMAM_VERSION=1.5
BUILD_TAG=-dev1
TAG=${MMAM_VERSION}${BUILD_TAG}
TAGGED_IMAGE=${IMAGE}:${TAG}
TARBALL=${IMAGE}-${TAG}.tar
ADIME_VERSION=2.2.1
ADIME_FULLNAME=adime-${ADIME_VERSION}
ADIME_TARBALL=${ADIME_FULLNAME}.tar.gz
ALLEGRO_VERSION=4.4.3.1
ALLEGRO_FULLNAME=allegro-${ALLEGRO_VERSION}
ALLEGRO_TARBALL=${ALLEGRO_FULLNAME}.tar.gz
ALOGG_VERSION=1.3.7
ALOGG_FULLNAME=alogg-${ALOGG_VERSION}.orig
ALOGG_TARBALL=alogg_${ALOGG_VERSION}.orig.tar.gz
